pipeline {
    agent { 
        dockerfile {
            dir 'DockerEnv/MQTT_HueController'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
